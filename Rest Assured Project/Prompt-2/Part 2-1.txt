src/test/resources/config/application-dev.properties

# =========================================
# Development Environment Configuration
# =========================================

environment=DEV

# Base URLs for Banking Microservices
base.url=https://api-dev.mybank.com
auth.base.url=https://auth-dev.mybank.com

# Service Endpoints
accounts.service.url=${base.url}/accounts/v1
transactions.service.url=${base.url}/transactions/v1
customers.service.url=${base.url}/customers/v1
payments.service.url=${base.url}/payments/v1
cards.service.url=${base.url}/cards/v1

# Authentication Endpoints
auth.token.url=${auth.base.url}/oauth/token
auth.refresh.url=${auth.base.url}/oauth/refresh
auth.revoke.url=${auth.base.url}/oauth/revoke

# TODO: Replace with actual dev credentials (use vault/secret manager in production)
auth.client.id=dev_client_12345
auth.client.secret=dev_secret_abcde
auth.grant.type=client_credentials

# Database Configuration (if needed for validation)
db.url=jdbc:postgresql://dev-db.mybank.com:5432/banking_db
db.username=test_user
db.password=test_pass

# Timeouts - Dev environment (shorter for faster feedback)
timeout.connect=3000
timeout.read=8000
timeout.write=5000

# Rate Limiting
rate.limit.requests.per.second=10

# Feature Flags
feature.schema.validation.enabled=true
feature.response.time.validation.enabled=true
feature.database.validation.enabled=false

# External Service URLs (if any)
email.service.url=https://email-dev.mybank.com
notification.service.url=https://notification-dev.mybank.com